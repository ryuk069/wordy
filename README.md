# will write something meaningfull later


Thinks that i learn making this project

1. After clicking the theme button, it remained focused, so when Enter was pressed later the browser interpreted it as a click on that focused button, causing changeTheme() to run again and toggle the theme.